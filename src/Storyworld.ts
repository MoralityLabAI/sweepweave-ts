import { Actor } from './Actor';

/**
 * STUB
 * This is a placeholder for the Storyworld class.
 * It will be replaced with a full implementation when Storyworld.gd is ported.
 */
export class Storyworld {
    public character_directory: Map<string, Actor> = new Map();
    public spool_directory: Map<string, any> = new Map();
    public encounter_directory: Map<string, any> = new Map();
}
